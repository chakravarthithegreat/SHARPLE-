# SHARPLE Environment Variables - Simple Format
# Copy each line exactly as shown into Netlify

VITE_API_URL=/api
VITE_APP_NAME=SHARPLE
VITE_APP_VERSION=1.0.0
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret_key
NODE_ENV=production
FRONTEND_URL=https://sharplee.netlify.app

# Instructions:
# 1. Copy each line above (except comments)
# 2. In Netlify, click "Add environment variables"
# 3. Paste the line - it will auto-split into Key and Value
# 4. Repeat for all 7 lines
# 5. Replace placeholder values with real ones
